import axios from 'axios';
import * as types from '../mutation/merchant_type';

const state = {
};

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {

}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
